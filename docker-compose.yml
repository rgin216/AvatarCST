services:
  mongo:
    image: mongo:7
    container_name: avatarcst-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - avatarcst-mongo-data:/data/db

volumes:
  avatarcst-mongo-data:
